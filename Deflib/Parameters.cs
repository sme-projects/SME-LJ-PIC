using SME;
using System;

namespace Deflib
{
    public enum Cache_size : ulong{
        n = 4,
    };

    public enum Magnitude_depth : ulong {
        n = 2,
    };

    public enum Force_depth : ulong {
        n = 9,
    };

    public enum Acceleration_depth : ulong {
        n = 5,
    };

    public enum Number_of_loops : ulong{
        n = 2,
    };
}